ROADMAP
=======

+ release 0.4.0
	+ (generic) support for floating point arithmetics
	+ register allocator support for FP registers
	+ conversion of immediates values
	+ i386 (SSE2)
		* arithmetics
		* branches
		+ conversions
		+ load/store
		+ argument reading
		+ argument passing
	+ AMD64 (SSE2)
		+ arithmetics
		+ conversions
		+ load/store
		+ argument reading
		+ argument passing
		+ branches
	+ SPARC
		+ arithmetics
		+ conversions
		+ load/store
		+ argument reading
		+ argument passing
		+ branches

+ release 0.4.1
	+ i386 (emulated)
		+ arithmetics
		+ conversions
		+ load/store
		+ argument reading
		+ argument passing
		+ branches
		+ conditionals

+ generic (release 0.5.x+)
	+ new putarg operation
	+ integrated disassambler
	+ separate test-cases
	+ input (i0, ..., i5) registers should be stored into their positions in the frame
	+ optimizations
	+ linear-scan reg. allocator
